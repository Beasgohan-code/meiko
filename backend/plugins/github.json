{
  "id": "github",
  "name": "GitHub",
  "description": "Search public GitHub repositories and code (keyless, rate-limited without a token; add a GitHub PAT in Settings for higher limits).",
  "base_url": "https://api.github.com",
  "auth": { "type": "bearer", "header": "Authorization", "prefix": "Bearer " },
  "enabled": true,
  "actions": [
    {
      "name": "github_search_repos",
      "description": "Search public GitHub repositories by keyword.",
      "method": "GET",
      "path": "/search/repositories",
      "parameters": {
        "type": "object",
        "properties": {
          "q": { "type": "string", "description": "Search query, e.g. 'language:python stars:>1000 llm agent'" },
          "per_page": { "type": "integer", "default": 5 }
        },
        "required": ["q"]
      },
      "query_params": ["q", "per_page"]
    },
    {
      "name": "github_search_code",
      "description": "Search code across public GitHub repositories.",
      "method": "GET",
      "path": "/search/code",
      "parameters": {
        "type": "object",
        "properties": {
          "q": { "type": "string", "description": "Code search query, e.g. 'def train_model language:python'" },
          "per_page": { "type": "integer", "default": 5 }
        },
        "required": ["q"]
      },
      "query_params": ["q", "per_page"]
    }
  ]
}
