{
  "id": "npm",
  "name": "npm Registry",
  "description": "Look up npm package metadata (latest version, description, dependencies) — free, keyless, official npm registry.",
  "base_url": "https://registry.npmjs.org",
  "auth": { "type": "none" },
  "enabled": true,
  "actions": [
    {
      "name": "npm_package_info",
      "description": "Get metadata for an npm package: latest version, description, homepage, license.",
      "method": "GET",
      "path": "/{package}",
      "parameters": {
        "type": "object",
        "properties": {
          "package": { "type": "string", "description": "npm package name, e.g. 'react'" }
        },
        "required": ["package"]
      },
      "path_params": ["package"]
    }
  ]
}
