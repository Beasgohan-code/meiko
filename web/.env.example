# Meiko Web App — environment config
# Leave VITE_BACKEND_URL empty during local dev (Vite proxies /api -> backend via vite.config.ts).
# Set it to your deployed backend URL for production builds, e.g.:
# VITE_BACKEND_URL=https://meiko-api.yourdomain.com
VITE_BACKEND_URL=
